const { print } = require('lccore')
const {mDirLoad} = require('../src/dir')

test('dir tests........', () => {expect(true).toBe(true)})
//test('mDirLoad', () => { mDirLoad('rest').then( res => { expect(res.length()).toBe(2)}).catch( err => {})})
